name: CI

permissions:
  contents: read

on:
  push:
    branches:
      - master
      - Hold+drag-stable
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  build:
    name: Build app
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v3

      # --- NEW STEP TO FIX BUILD ---
      - name: Create local.properties
        run: |
          echo "anilist.id=${{ secrets.ANILIST_ID }}" >> local.properties
          echo "anilist.secret=${{ secrets.ANILIST_SECRET }}" >> local.properties
          echo "mal.id=${{ secrets.MAL_ID }}" >> local.properties
          echo "mal.secret=${{ secrets.MAL_SECRET }}" >> local.properties
          # Add dummy values if secrets are not set yet to prevent build failure
          sed -i 's/=${{ secrets.* }}/=dummy_value/g' local.properties 

      - name: Build app
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: animetail-debug
          path: app/build/outputs/apk/debug/*.apk
